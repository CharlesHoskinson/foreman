# Devlog 2026-08-02 — Council skill localization and first three-domain review

## 1. What actually happened, in order

The worktree
`foreman-wt-council-v030-20260802-implement-council` started from base
`bb3124ff5c053ad0bb9b17a7aa9108ecaee7b05a`. That commit already held the
Council localization plan and the unsquashed `components/council/` subtree.

Task 2 ran before Task 1 on purpose. The operator required every
Council-labeled iteration to use the real Council skill. `agy` was already
authenticated on the host. Setup admission for `agy` stayed open.

Implement work added the canonical skill under `skills/council/`, the thin
Antigravity wrapper under `.agents/plugins/council/`, the
`council` inventory row in `env/tool-check.sh`, and
`tests/council-localization.bats`. All of that stayed uncommitted.

The first Council review built bundle
`/tmp/council-skill-r1-bundle/bundle.json` with
`base_sha == head_sha == bb3124ff5c053ad0bb9b17a7aa9108ecaee7b05a`.
The advisory result is `/tmp/council-skill-r1-advisory.json`. Outcome:
`quorum_not_met`. Admissible verdicts: 2. Domains: Google and Anthropic.
Google approved. Anthropic requested changes. Codex was inadmissible.

Anthropic findings forced rework on fail-capable localization tests and on
root docs-gate exclusions for the imported Council subtree. Independent
verification after rework reported:

| Command or artifact | Result |
|---|---|
| `bash tests/run.sh tests/council-localization.bats tests/docs-check.bats tests/plugin-drift.bats` | 22/22 pass (`/tmp/council-rework-green.out`) |
| `corepack pnpm --dir components/council -s verify` | 114/114 tests in 9 files |
| `bats tests/line-endings.bats` | 6/6 ok |
| `bash skills/foreman/scripts/docs-check.sh` | pass (all five tools) |
| `python3 .../quick_validate.py skills/council` | Skill is valid |
| `agy plugin validate .agents/plugins/council` | ok, 1 skill processed |
| `git diff HEAD --check` | clean |

The full Foreman branch gate then ran with
`FOREMAN_CI_BATS=1 bash tools/ci-local.sh`. It reported 629 pass, 0 fail,
19 skip, and 648 tests. It ended with
`CI-LOCAL RESULT PASS gates_failed=0`. Council did not approve this round.
The product tree is still uncommitted.

This session records those facts in append-only operational docs only. It
does not change product code, tests, configs, the Council subtree, or
release authority.

## 2. What went right

| Evidence | Result |
|---|---|
| First live three-domain Council loop | Produced a typed `quorum_not_met` outcome with preserved dissent |
| Google-domain admissible verdict | `approved` with exact bundle identity echoed |
| Anthropic-domain admissible verdict | `changes_requested` with concrete file findings |
| Targeted Foreman slice after rework | 22/22 pass in `/tmp/council-rework-green.out` |
| Council package verify | 114/114 |
| Line-ending tests | 6/6 |
| Root docs-check after narrow exclusions | pass |
| Skill and Antigravity validation | both passed |
| `git diff --check` | pass |
| Actionable dissent | Anthropic findings drove real test and docs-gate rework |
| `FOREMAN_CI_BATS=1 bash tools/ci-local.sh` | 629 pass, 0 fail, 19 skip, 648 tests; `gates_failed=0` |

## 3. What went wrong

| What it claimed | What was true |
|---|---|
| The first Council bundle reviewed the Task 2 work | `base_sha` equalled `head_sha`, so the committed round had no skill diff. Uncommitted and untracked files were outside exact bundle identity |
| Codex returned a reviewable verdict | Transcript text contaminated the verdict body. The response was inadmissible |
| Localization tests were fail-capable against the Council inventory row | Assertions matched `ok` / `missing` and generic words (`model`, `provider`, `advisory`) anywhere in multi-row blocks or prose |
| The first docs-check failure surface was the full markdownlint set | `docs-check.sh` tails markdownlint output. The first capture hid most nested imported findings. The measured full set was far larger than the last-20-line view |
| Council approved the round | Outcome was `quorum_not_met`. Anthropic requested changes. Codex was inadmissible |
| Task 2 is finished end to end | Implementation exists in the worktree. Immutable committed-bundle re-review has not run |
| The v0.3.0 release branch holds this work | The release branch does not contain these uncommitted worktree changes |

## 4. Standing rules that came out of today

- Build a Council bundle only from committed SHAs. Require
  `base_sha != head_sha` and a real diff content hash before review.
- Capture Codex final message alone. Keep stderr separate. Treat transcript
  contamination as inadmissible, not as a soft failure.
- Bind localization claims to the exact Council inventory row and to named
  protocol sections. Do not match cross-row status tokens or generic prose
  words.
- When docs-check fails, inspect the full tool log. Do not trust a tailed
  markdownlint slice as the complete finding surface.
- One admissible `changes_requested` blocks progress. A majority approval
  does not override it.
- Do not call Task 2 release-ready until an immutable committed bundle
  clears Council without unresolved dissent.

## 5. Where things stand

Task 2 implementation is present and locally verified in this worktree.
Task 1 (`agy` Setup admission) is incomplete. Tasks 3–7 are not started.
The first Council outcome remains `quorum_not_met`. Product changes are
uncommitted. The full Foreman branch gate passed with the command and totals
recorded above. Council has not approved this round. Append-only operational
docs now record the localization and the first review.

## 6. Resume instructions

1. Review the uncommitted product tree. Do not assume it is on
   `release/v0.3.0-council`.
2. Re-run the full Foreman gate after any candidate-tree change.
3. Commit the complete Task 2 round in one clean commit set. Include skill,
   wrapper, tests, tool-check inventory, baselines, and docs-gate configs.
4. Build a new immutable Council bundle from the new `base_sha` / `head_sha`
   pair. Confirm they differ.
5. Invoke Council again. Keep every admissible `changes_requested` finding
   until fixed.
6. Leave Task 1 incomplete until Setup admits authenticated `agy`.
7. Do not claim Council approval or full-gate green until those commands
   actually pass.

---

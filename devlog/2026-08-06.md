# 2026-08-06

Written from `git log --since="2026-08-06 00:00"`, not from memory.

## 1. What shipped

Ten commits, two pull requests merged, one branch pushed without a PR.

| Commit | |
|---|---|
| `04fe341` | reconcile v0.2.9.0 authority onto `main` and land the v0.3.0 program |
| `b0835e3` | credential-profile identity swaps made inode-deterministic |
| `06c2d24` | prettier on four files blocking the Council gate |
| `5507c61` | re-pin the stale preflight bundle identity anchor |
| `f30a39e` | clear the docs gate and one bats failure in the shared gate |
| `0266e56` | tolerate the missing Windows directory-fsync barrier |
| `67a5a3b` | tracked-delete fixtures made platform-native |
| `12391ac` | make credential profiles work on Windows |
| `39c555e` | close the last three gates-windows failures |
| `7bd0428` | port repo-hygiene to TypeScript, add a mode-change declaration |

PR #28 → `main` (green on both gates). PR #29 → the v0.3.0 branch (8 commits).
`fix/repo-hygiene-typescript-port` is pushed and verified but has no PR.

Gate movement: **`gates-windows` 81 test failures → 0**, whole workflow PASS.
**`gates-linux`** failed at step 4 for eleven consecutive runs → steps 4–11 pass,
`gates_failed` 3 → 1. The remaining one is the lane-queue contract reconciliation.

## 2. What we learned

Only two of ten commits changed production behaviour. The rest were records, fixtures and
platform guards. A day that moved CI from unusable to nearly clean moved the *product* almost
not at all — worth saying plainly, because the same shape repeated as a program: the v0.3.0
backlog is 819 open tasks and over half of it is the tool observing itself.

## 3. What went wrong

The section this log exists for.

| What it claimed | What was true |
|---|---|
| The lane-queue port "does not reproduce the POSIX quoting dialect, `shell_command` override classification, or `FORCE_MISSING` fallback" — recorded in `f30a39e` **and** PR #29 | The `add` verb gained five mandatory Endstop flags. Behaviour was ported *and extended*. Five artifacts encode two contracts. The cause was inferred from test names and never verified. Caught by an independent lens measuring the tree |
| A test proved an identity gate refuses a swapped directory | On ext4 the inode is reused, so the simulated attack never happened. It had been passing on tmpfs for the wrong reason |
| Two assertions proved absolute paths do not leak | `JSON.stringify` escapes backslashes, so on Windows they would have passed while testing nothing — one careless edit from a permanent no-op |
| The preflight bundle "remains exactly 845,594 bytes with SHA-256 `35fea54f…`" (PR #27) | Not true since `d60ebdc`. Three reviewed commits moved it; the constant was never updated |
| `gates-linux` had one small test problem | Five defects in series. `set -e` meant the first red step hid four more; eleven identical runs looked like one issue |
| `fast-check` is a root devDependency (asserted in agent briefs) | Declared only in `components/council`. The `node_modules` copy is a transitive of `effect`, major-behind |
| `tests/baseline.tsv`: 21 passes for lane-queue | 14 pass / 2 skip / 7 fail. `tests/run.sh` defaults to shadow mode — the instruments are pointed at a disarmed trigger |
| The suite result is a verdict | Globs are unquoted (`sh` has no globstar, works by accident), `node --test` exits 0 on zero tests, 168 bare `if (…) return` count as passes, 4 of 5 skips are anonymous |

The class: **inference recorded as diagnosis.** Four of these were conclusions that sounded
right and were never executed against the tree. The two that were caught, were caught by
someone else running the code.

Second class, equally consistent: **a gate that has not run is not a green gate.** The prettier
drift and the bundle-anchor drift both accumulated precisely while the step that would have
caught them was unreachable.

## 4. Where it stopped

Mid-design, deliberately. See `docs/superpowers/specs/2026-08-06-v030-checkpoint.md` for the
settled decisions, the unfixed verified defects, and what is still undecided. Nine review
lenses are preserved under `docs/research/2026-08-06-testing-council/`.

Resume with `fm-session.py recover`, then that checkpoint.

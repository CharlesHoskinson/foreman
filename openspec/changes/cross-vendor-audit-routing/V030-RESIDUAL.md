# v0.3 residual: audit routing expansion

v0.2.9 ships one shared model-family selector and cross-family refusal tests.

v0.3 preserves these requirements:

- Apply the family check at each remaining gate-evaluation boundary.
- Record the selected auditor fields in every soft-mode artifact.
- Add the optional dual-audit path and its disagreement policy.
- Re-run the routing controls for each new vendor family.

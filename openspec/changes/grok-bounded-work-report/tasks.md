## Research and design

- [x] Inspect all 27 local Grok user-guide chapters.
- [x] Read all 42 model-card pages through PixelRAG images.
- [x] Capture the dated public documentation corpus.
- [x] Finish and reconcile whole-corpus reading coverage.
- [x] Record native/API differences and hard-budget enforcement gaps.
- [x] Specify coding and reporting as separate phases.
- [x] Obtain user approval for the process-separated design and GPT-6 implementation.
- [x] Obtain user approval to modify the pinned native Grok source.
- [x] Switch remaining work to GPT-5.6 Sol at the user's request.
- [ ] Establish native hard-budget feasibility before live dispatch.

## Implementation after design review

- [ ] Add failing fixtures for phase order, identity, cancellation, cleanup, and unknown usage.
- [x] Reject unsupported hard native budgets before paid dispatch.
- [x] Separate the internal ACP protocol from public admission without a public bypass.
- [x] Reject JSON-shaped progress without session-bound and model-bound structured terminal metadata.
- [x] Preserve shared protocol fixtures and test public qualification refusal before credential or process acquisition.
- [x] Add a trusted native sampler veto before streaming and nonstreaming HTTP execution.
- [x] Test native denial, error propagation, body identity, and in-memory hook cloning with local fixtures.
- [x] Verify shell denial remains terminal before compaction and salvage.
- [x] Preserve the readable denial message and exact reason in shell completion metadata.
- [x] Preserve terminal admission-denial display through the pager, including contradictory HTTP status.
- [x] Complete the full CLI compilation check for the admission-veto milestone.
- [x] Preserve the same trusted hook across in-memory session, model, subagent, and auxiliary sampler reconstruction.
- [ ] Qualify every auxiliary request path, including direct tokenizer and web-search HTTP clients.
- [ ] Implement the qualified native resource-control path without increasing limits.
- [ ] Separate ACP protocol handling from work/report orchestration.
- [ ] End coding authority before immutable evidence capture.
- [ ] Enforce a no-tools reporting boundary.
- [ ] Validate only the exact structured terminal report.
- [ ] Preserve one effect, reservation, deadline, and aggregate resource ledger.

## Acceptance

- [ ] Pass protocol, namespace, resource, and exact-byte qualification fixtures.
- [x] Pass current type checking and compiled build for the safety milestone.
- [ ] Obtain fresh independent candidate review.
- [ ] Pass one complete live qualification with Grok 4.6 and the native account.
- [ ] Install the reviewed runtime through the existing package route.
- [ ] Resume Moriarty implementation without changing its acceptance gates.

## Safety milestone scope

GPT-6 Astra implemented the admission and terminal-output repairs.
The public Grok adapter rejects starts because hard-budget enforcement remains unavailable.
Internal protocol fixtures do not establish live qualification or complete process separation.
The full controller, immutable evidence transition, and no-tools reporter remain open.

## Native source milestone scope

The native base is `482711333c7195dc16a272777f86086d615e2afb` from `xai-org/grok-build`.
Its version crate reports 1.0.32, not the installed 1.0.30 binary.
GPT-6 wrote the sampler admission candidate before the user switched remaining work to GPT-5.6 Sol.
The hook supplies a typed local veto at both sampler HTTP execution methods.
It is not connected to Foreman's budget authority or native startup admission.
Serialization drops the hook. The propagation candidate preserves an existing
authority through in-memory session, model, subagent, and auxiliary sampler reconstruction.
The host verified the propagation regressions and full CLI compilation check.
Allowed requests still lack qualified redirect, replay, token, billing, and cross-process accounting guarantees.
The Foreman public adapter therefore remains closed.

Sol corrected the shell completion payload to use `message`, `error_kind`, and `reason`.
The shell reader preserves each closed denial reason.
The host reran both admission regressions successfully after this correction.
These tests do not establish native budget enforcement or live qualification.

The full CLI check exposed a missing pager error case.
Sol added a terminal display class with no retry or compaction advice.
The host reran the focused pager test and `cargo check -p xai-grok-pager-bin` successfully.
This check covers the admission-veto milestone before session-propagation changes.
It does not produce an installed release binary or establish live qualification.

Sol reported four passing propagation regressions after four behavioral failures.
Sol also reported a successful full CLI compilation check after propagation changes.
The host independently ran `cargo +1.94.0 test -p xai-grok-shell --lib admission --locked --offline -j4`.
All 18 selected tests passed, including four propagation regressions and both denial regressions.
The host then ran `cargo +1.94.0 check -p xai-grok-pager-bin --locked --offline -j4` successfully.
These results apply to the propagation candidate, not a release binary or live qualification.
Direct tokenizer and web-search HTTP requests remain outside sampler admission.

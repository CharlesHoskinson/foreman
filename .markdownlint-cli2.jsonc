{
  // Foreman docs style: permissive on line length/HTML (doctrine tables), strict on structure.
  // MD024 siblings_only: repeated subsection headings under different parents are intended
  // (chronological logs, per-finding reports); the rule still fires on same-parent duplicates.
  // MD046 fenced: the repo uses ~2080 fenced blocks. Unset, the rule INFERS the
  // expected style from each file's first block, so bugeventlog.md (which opens
  // with an indented one) reported all 13 of its fenced blocks as wrong. Pinned,
  // total findings drop 79 -> 67 and the rule still fires on genuine departures.
  "config": { "default": true, "MD013": false, "MD029": { "style": "ordered" }, "MD033": false, "MD041": false, "MD060": false, "MD024": { "siblings_only": true }, "MD046": { "style": "fenced" } },
  // Archived evidence is exempt: they are archives of what happened, and editing an archive to satisfy a style linter falsifies it.
  "ignores": [
    "skills/scrapling/**", "skills/graphify/**", "skills/superpowers/**",
    "docs/research/**", "sandbox/**", "node_modules/**", "**/node_modules/**", "FOREMAN_REPORT.md", "FOREMAN_REPORT.json",
    "graphify-out/**", ".foreman/**", ".harness/**", ".superpowers/**", "openspec/changes/**",
    // Nested Council OpenSpec change records: same archive class as root openspec/changes/**
    // (do not edit imported OpenSpec history merely to satisfy the root linter).
    "components/council/openspec/changes/**",
    // Nested root-parity research archives and superpowers plan/spec dumps inside the
    // imported subtree. Root already ignores docs/research/** and skills/superpowers/**.
    "components/council/docs/research/**",
    "components/council/docs/superpowers/**",
    // Embedded OpenSpec CLI skill/command copies shipped inside the Council import
    // (.claude/.codex/.gemini). Not Council human docs; not a blanket subtree ignore.
    "components/council/.claude/**",
    "components/council/.codex/**",
    "components/council/.gemini/**",
    "docs/evidence/**"
  ]
}

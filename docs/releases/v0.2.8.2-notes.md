# v0.2.8.2 release notes

Status: released on 2026-08-03. Annotated tag `v0.2.8.2` and the GitHub release
target exact commit `076c014a8123d8df341b8037b3be5756c7cf6354`.

## Why this release exists

Foreman had spent most of its lane runs on Foreman itself. The v0.2.9 plan
expanded that loop with formal, graph, governance, and statistical work before
Foreman had completed one external implementation. v0.2.8.2 stops that release
and uses an external Gobox pilot as the forcing function.

The withdrawn work is preserved by annotated tag `v0.2.9-preserve`, which
targets `04f369541febf648ef39dcb8fedd930e8b600d3f`. The tag is a recovery point,
not an active roadmap.

## External pilot result

A cold Grok worker changed Gobox
`scripts/install-user-dosbox.sh`. The change replaces a silent, incomplete
package list with a derived recursive runtime closure and fails closed on
resolver, download, extraction, linkage, and wrapper-test errors.

- Worker commit: `b0519b25c1c8a5fe46fbd9de27df34775e98c0b0`
- Gobox merge: `24bd736dd92050d886ef59a0237aa2c949acf593`
- Pull request: [CharlesHoskinson/gobox#1](https://github.com/CharlesHoskinson/gobox/pull/1)
- Target gate: Gobox `make check`
- Independent review: Codex cold diff audit, with a hard stop at two reworks

The product commit changes one Gobox file. Harness and report artifacts were
not included in that commit.

## Foreman portability changes

Foreman PR [#7](https://github.com/CharlesHoskinson/foreman/pull/7) merged as
`525cfb050df8afccdad770b8b3aefb79bb0f8183`.

The merge:

- selects an exact soft-mode worktree from the worktree index when hard-mode
  `meta.json` is absent;
- keeps pristine-commit archive verification;
- detects target-owned Make and Go gates;
- separates the Foreman tool root, target repository root, target worktree,
  and run-scoped SessionDB;
- runs workers and gates in the selected external worktree;
- resolves Foreman readiness, launcher, and WSL resources from the Foreman
  checkout;
- documents the external-target operating boundary;
- fixes a Linux trace expression that failed when a generated target path
  contained the letter `n`.

The PR added or updated focused Bats coverage for external checks, target-root
selection, session isolation, lane working directories, and the trace parser.

## Linux NATS coverage

Foreman PR [#10](https://github.com/CharlesHoskinson/foreman/pull/10) merged as
`09e07159ef94b945a6acc2b6c1333864d13291ea`.

The Linux workflow previously lacked `nats-server` and `nats`, so all 12 NATS
integration tests were skipped. The workflow now installs checksum-pinned NATS
Server 2.14.4 and NATS CLI 0.4.0 archives, verifies their exact versions, and
runs the integration slice. All 12 NATS tests passed at PR #10 head
`af2762c9b1d2989d50d3efe2963ea9f97119a3c0`; both Linux and Windows workflows
completed successfully.

## Deterministic formal-runner assets

The first exact-main Windows run after PR #12 failed before model evaluation.
Quint 0.32.0 tried to discover its Rust evaluator through the unauthenticated
GitHub Releases API, received a rate-limit response, and classified all 19
commit-tier rows as `ERROR`. The models did not produce counterexamples.

Release PR [#14](https://github.com/CharlesHoskinson/foreman/pull/14) removes
that hidden first-run dependency. The Linux, Windows, and standalone formal
workflows download the evaluator from its stable release asset URL, verify the
publisher-recorded SHA-256 digest, and place it in Quint's versioned cache
before a model runs. PR #14 Windows run `30802535713` passed in 5 minutes 4
seconds. Its uploaded formal report records 19 rows run, 19 matched, and 0
failures. Both standalone formal runs on the PR head also passed.

PR #14 head `c72db1b9b2b02c80b404cefe95b953cedcde609f` merged as exact main commit
`074f3c4d9ac8a02a5cf10cb1b2892ae7d81f59a6`.

## External-portability defect list

The pilot produced this backlog:

| Defect | v0.2.8.2 disposition |
|---|---|
| Soft checks required hard-mode `meta.json` | Fixed in PR #7 |
| Soft worktree selection was ambiguous | Fixed with exact index selection |
| Foreman tool root and target root were conflated | Fixed in `lane-run.sh` |
| Target checks could fall back to Foreman-specific documentation checks | Fixed for Make, Go, and explicit overrides |
| Target state could use the Foreman SessionDB | Fixed with a run-scoped target database |
| Worker and gate working directories could be inherited from the caller | Fixed with the selected target worktree |
| Linux mkdir trace parsing rejected paths containing `n` | Fixed before merge |
| Queue readiness can race with a refused enqueue | Open; see `docs/RESIDUALS.md` |
| Harness and report artifacts enter the target worktree | Open; product commit excluded them |
| Changed-file telemetry can mix product and harness artifacts | Open |
| The external matrix contains one repository and one workload | Open by design for this release |

## Scope reset

The following items are not v0.2.8.2 release criteria:

- the formal model plane;
- the graph and knowledge-plane package program;
- Tier 2 live vendor statistics;
- completion of every v0.2.9 OpenSpec package;
- a repository-wide historical measurement refresh;
- Superpowers extraction;
- the `dev/foreman-v1` session-transport branch;
- Council v0.3 and the TypeScript migration.

No bulk worktree deletion is part of this release. The current cleanup log is
`docs/releases/v0.2.8.2-cleanup-log.md`.

## Verification evidence

At Foreman PR #7 head
`4853087a4b5320dd15ea697ec2435ceec4841129`:

- `FOREMAN_CI_BATS=1 bash tools/ci-local.sh` completed with 685 passed,
  0 failed, and 19 skipped Bats cases;
- the focused setup, tool-check, and lock suite passed 29 of 29 cases;
- GitHub `gates-linux` and `gates-windows` completed successfully.

These PR-head measurements were preliminary evidence. The final exact-commit
results are recorded below.

At release-record PR [#12](https://github.com/CharlesHoskinson/foreman/pull/12)
head `86ea42233a5b14c9cd3dabc0e91b441c40ba93e6`:

- `gates-linux` completed in 20 minutes 25 seconds;
- `gates-windows` completed in 5 minutes 24 seconds;
- the Linux aggregate was 685 passed, 0 failed, and 19 skipped Bats cases;
- the uploaded Linux slice report records `tests/nats-bridge.bats` as 12
  passed, 0 failed, and 0 skipped; and
- OpenSpec, documentation, dependency, and repository-hygiene gates passed.

PR #12 merged as
`d9eafbb17bcf0d968d8b4c6998ce7476d60dc8c3`. Later exact-commit evidence
superseded these preliminary results.

At exact main commit
`074f3c4d9ac8a02a5cf10cb1b2892ae7d81f59a6`:

- standalone formal run `30805814088` reported 19 run, 19 matched, 17 skipped,
  and 0 failures;
- Windows run `30805814536` passed its required gate; its non-gating
  line-ending probe reported five passed and one failed, and plugin drift
  reported three passed and zero failed; and
- Linux run `30805814811` reported `CI-LOCAL RESULT PASS`, 685 passed, 0
  failed, and 19 skipped Bats cases, including 12 of 12 NATS cases passed.

A clean source-only checkout then exposed one release-command precondition.
The launcher fallback test requires the ignored
`launcher/dist/foreman-launch` build output. The GitHub workflow already builds
that legacy Bun launcher before the suite. The local release command now states
the same build step explicitly instead of adding new Bash test logic. After a
source build, the focused slice passed 3 of 3. The complete clean-boundary run
then passed 685 cases, failed 0, skipped 19, and reported every CI-local gate as
passed.

At exact release commit
`076c014a8123d8df341b8037b3be5756c7cf6354`:

- the local release gate reported `CI-LOCAL RESULT PASS`, 685 passed, 0 failed,
  and 19 skipped Bats cases;
- all 12 NATS cases passed;
- hosted Linux run `30819569495` passed;
- hosted Windows run `30819569488` passed;
- the authority graph recorded 1,434 nodes, 2,618 links, 115 communities, and
  no duplicate or dangling records; and
- the annotated local tag, remote tag, and GitHub release resolve to the same
  commit.

Release: <https://github.com/CharlesHoskinson/foreman/releases/tag/v0.2.8.2>

## Known limits

Read `docs/RESIDUALS.md` before claiming support beyond the tested pilot. In
particular, Windows Bats remains a non-gating probe, queue admission still has
a socket-readiness race, and the repository-wide Node.js and TypeScript
migration is not complete.

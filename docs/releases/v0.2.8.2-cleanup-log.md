# v0.2.8.2 cleanup and destruction log

This is the current repository copy of the cleanup register. It replaces stale
counts and proposed commands with exact dispositions. The detailed 2026-08-02
inventory remains run-local historical evidence. It is not execution authority.

## Rules

- Register an action before execution.
- Preserve tracked content before deletion.
- Use one reversible commit for each tracked cleanup package.
- Do not delete a worktree until its tracked, untracked, ignored, ownership,
  symlink, remote, and recovery state is current.
- Do not replace generated knowledge until the replacement records its source
  commit and passes validation.
- Do not rewrite SessionDB history to remove an old release name.

## Preservation

The annotated remote tag `v0.2.9-preserve` targets
`04f369541febf648ef39dcb8fedd930e8b600d3f`. It preserves the withdrawn release
tree before tracked release-document cleanup.

Recovery example:

```bash
git show v0.2.9-preserve:docs/releases/v0.2.9.0-notes.md
```

## Current register

| ID | Target or action | Current disposition |
|---|---|---|
| `DST-0001` | Historical orphan Foreman workers from the pivot prompt | `no_op`; the stale PIDs had already exited |
| `DST-0002` | `formal/`, its workflow, and related package | `deferred`; no tracked formal path is deleted in v0.2.8.2 |
| `DST-0003` | Six graph and knowledge OpenSpec packages | `deferred`; shipped graph utilities and tests remain |
| `DST-0004` | Tier 2 vendor-statistics machinery | `deferred`; no live Tier 2 result is claimed |
| `DST-0005` | Wrong-premise OpenSpec packages | `deferred`; disposition needs a later exact package change |
| `DST-0006` | Superseded active v0.2.9 release records | `executed` in this cleanup branch as described below |
| `DST-0007` | Non-main worktrees | `deferred`; no bulk removal, commit, or stash occurred |
| `DST-0008` | Council skill link into a worktree | `executed_reversible`; the official installer repointed all three links to merged `main`, and the old worktree remains intact |
| `DST-0009` | Stale `graphify-out/` | `executed_reversible`; the exact-release authority graph replaced it after validation, and the old graph remains quarantined intact |
| `DST-0010` | `skills/superpowers/` extraction | `rejected` for this sprint until a complete dependency manifest passes |
| `DST-0011` | Accidental run-local SessionDB duplicate | `deferred`; no database file is deleted by this branch |
| `DST-0012` | Stale GitHub pull requests #4, #5, and #6 | `executed`; closed with superseding links, while branches, commits, and tags remain |
| `DST-0013` | Accidental host-default SessionDB and sidecar | `executed_reversible`; three unique facts were recovered, the stale checkpoint was replaced, and both files were moved to the exact quarantine path below |
| `DST-0014` | Failed staged Graphify rebuild at `/home/charl/.foreman/graphify-rebuild-v0282.B86kNc/` | `retained_not_promoted`; semantic chunk 4 timed out, the process stopped, and the staging tree remains recoverable until its verified successor is promoted |
| `DST-0015` | Graphify attempt without the required executable path | `retained_not_promoted`; the 104 KiB attempt remains at `/home/charl/.foreman/graphify-rebuild-v0282.JQ08ZC/attempt-1-path-missing/` |
| `DST-0016` | Unfiltered Graphify candidate | `retained_not_promoted`; it mixed 241 historical documents and 172 vendored skill files into the current corpus |
| `DST-0017` | First authority-filter Graphify candidate | `retained_not_promoted`; the filter excluded live dependencies and was replaced before publication |
| `DST-0018` | Exact-main gate attempt with a stale local remote-tracking ref | `retained_invalid`; the 529-byte log is evidence of a rejected measurement, not a release result |
| `DST-0019` | Two inaccurate active design documents | `executed_reversible`; Git history preserves both deleted files |
| `DST-0020` | Existing live `graphify-out/` | `executed_reversible`; the validated exact-release graph was promoted atomically, and the old graph remains quarantined intact |
| `DST-0021` | Stale active graph, path, and fixed-suite instructions | `executed_reversible`; current doctrine now names the live checkout, derived graph, current command, and measured-suite rule |
| `DST-0022` | Graphify 0.9.15 vendored skill with Graphify 0.9.32 installed | `deferred`; exclude the vendored copy from authority and update it only through a separate vendored-skill change |
| `DST-0023` | Broad active-OpenSpec Graphify candidate | `retained_not_promoted`; it converted a withdrawn v0.2.9 metric package into current graph authority |
| `DST-0024` | First narrowed Graphify candidate | `rejected_rebuilt_in_place`; its corpus still admitted dated devlogs and the withdrawn v0.2.9 poster |
| `DST-0025` | Stale nested Council graph outputs | `executed_reversible`; PR #18 removes the generated report, HTML, and JSON after recording their hashes and recovery commit |
| `DST-0026` | Superseded Council stack pull requests | `executed_reversible`; PR #8 is merged and PRs #9, #11, #13, #15, and #17 are closed with branches and commits retained |

## Executed tracked-document cleanup

The release cleanup performed these tracked actions:

1. Delete `docs/releases/v0.2.9.0-notes.md`.
2. Replace `ROADMAP.md` with the v0.2.8.2 roadmap.
3. Replace `checklist.md` with three release criteria.
4. Replace `docs/RESIDUALS.md` with current residuals.
5. Add `docs/releases/v0.2.8.2-notes.md`.
6. Label `bugeventlog.md` as historical and non-authoritative.
7. Label `docs/research/vnext/START-HERE.md` as historical and superseded.
8. Add `docs/evidence/README.md` to define evidence authority.
9. Add this current cleanup log.
10. Delete `docs/design/PROMPT-project-registry.md`, which named an obsolete
    worktree as the integration path.
11. Delete `docs/design/test-cleanup-roadmap.md`, which described the active
    Bats gate as disabled and deadlocked.
12. Add `.graphifyignore` as the current-authority corpus boundary.
13. Recover the owner-requested TypeScript migration checklist, repository
    Iron Rule, and OpenSpec package without the withdrawn release's
    package-matrix edit.
14. Correct stale graph commands and tracked-graph claims in `.gitignore`,
    `CLAUDE.md`, `README.md`, the active Foreman skill, and active OpenSpec;
    replace the stale Grok host version, fixed suite counts, and obsolete
    base-checkout paths with current measurements or rules that require them;
    require an exact graph commit match and semantic extraction after
    documentation or mixed changes; and remove the withdrawn v0.2.9 release
    claim from the active Council OpenSpec package.
15. Exclude deferred OpenSpec packages from the current-authority graph. Keep
    only the approved Council v0.3 and Node.js with TypeScript packages in the
    graph corpus. Exclude dated devlogs and the withdrawn v0.2.9 poster. The
    source records remain in Git and are not deleted.

Git and `v0.2.9-preserve` provide recovery for every tracked action.

## Executed exact-release graph replacement

The authority-filtered graph was rebuilt from exact release commit
`076c014a8123d8df341b8037b3be5756c7cf6354`. Validation found 1,434 nodes,
2,618 links, 115 communities, no duplicate nodes, and no dangling links or
hyperedge references. The replacement was then promoted to `graphify-out/`.

The prior live graph was moved intact to
`/home/charl/.foreman/quarantine/graphify-live-stale-20260803T135701Z/graphify-out`.
The replacement graph SHA-256 is
`90ff4b98158f1c0256e357a2f216f54c6829687fca0467b37537c2c1ad0bd448`.
This replacement did not delete the prior graph.

## Executed host-only cleanup

- The obsolete `/home/charl/.foreman/hourly-v029-checkpoint.sh` writer was
  stopped by its exact process group and removed after its lock identity was
  verified. It was replaced by the active
  `/home/charl/.foreman/hourly-checkpoint.sh` detached, lock-owning writer.
  SessionDB fact 409 records the replacement and script hash.
- Two orphan Node processes created by an intentionally RED Council process
  test were terminated by their exact PIDs after their command lines were
  recorded. The corrected GREEN tests leave no child process behind.
- The hourly writer used its host working directory for one checkpoint and
  wrote fact 4 to the non-canonical `/home/charl/.foreman/session.db`. The
  checkpoint was rejected and replaced by canonical fact 422. Unique host
  facts 1 through 3 were recovered as canonical facts 423 through 425. Those
  records were then linked to their current successors, so canonical recovery
  does not present the historical Council dispatch or watchdog snapshots as
  current guidance. Fact 428 records the links. The corrected writer uses the
  explicit canonical database, records the final release worktree, and runs as
  a detached, lock-owning process under init.
- The accidental host database and sidecar were moved, without deletion, to
  `/home/charl/.foreman/quarantine/sessiondb-host-default-20260803T094854Z/`.
  Their SHA-256 digests are
  `ec25f86922bd10d9be2348d15c08f2b340426e7d5ed6bcb1cb7bffb90c76fb7c`
  and
  `8720c2831f7c640346747e795e56b0a2097caf686bb34a92e4cfac9127d5805e`.
  Canonical fact 426 records the writer identity and recovery mapping.
- The canonical `main` checkout was realigned to `origin/main` after its
  SessionDB sidecar was stored as stash object
  `32b8570dd9ba646ba189041b36e4af6a0c3cd1b7`. The pre-realignment sidecar is
  also retained under
  `/home/charl/.foreman/quarantine/main-realign-20260803T.J4dTa8/`.
- A Gobox-era `DRIFT-ALERT.md` was moved to
  `/home/charl/.foreman/quarantine/drift-alert-gobox-20260803T1110Z/`. Its
  SHA-256 digest is
  `ee8c85332d71567c71ce23f58f09720ba691a5c995e05bc0c9ba299de09770ad`.
  The active v0.2.8.2 watchdog can now create only a fresh alert.
- The hourly checkpoint script now records the release-record, exact-main
  verification, authority-graph source, and active Council provider
  worktrees. The updated script passes Bash syntax and ShellCheck.
- The drift watchdog produced one `CHURNING` verdict from a stale evidence
  contract that still measured the obsolete cleanup worktree and omitted the
  required final release record and authority graph. Canonical fact 453
  supersedes that verdict. The corrected objective measures PR #16, its
  worktree, the graph source commit, and graph candidate state before tag and
  release. The detached loop also restores the user-bus environment before it
  reads the Graphify service. Thirty-one watchdog tests, Bash syntax, and
  ShellCheck pass. The script SHA-256 digest is
  `0ee28ce5fe6b6c518d66f4cce8cfdff0f58d77521de08cea6673182d58e25f23`.
- After Council PR #18 merged as `87e6a58`, the official installer repointed
  the Claude, Agents, and Grok Council skill links to
  `/home/charl/foreman/skills/council`. All three installed skill files match
  the merged source SHA-256
  `b4e1c9cf52d27f319d1abf36a3f464419e8aa2b3ab53aae035ed534ba7cd9d95`.
  The old Council worktree was not deleted.

These host actions did not delete repository content.

## Rejected graph candidate

The staged Graphify rebuild from source commit
`e862eaef3c84e53a7825d0387bc7fff8382240c4` failed closed. Semantic chunk 4
exceeded its 600-second limit. The extraction process stopped with exit 130
after the candidate became ineligible for promotion. The staging root is
`/home/charl/.foreman/graphify-rebuild-v0282.B86kNc/`. It contains no
`graph.json`, and the live `graphify-out/` directory was not changed. Keep the
staging root until a fresh graph from the final release candidate passes all
validation and promotion checks.

Five later candidates under
`/home/charl/.foreman/graphify-rebuild-v0282.JQ08ZC/` also failed closed before
publication. Attempt 1 lacked the required executable path. Attempt 2 indexed
804 files and 1,386,127 words, including historical release material and
vendored skill copies. Attempt 3 over-excluded live dependencies. Attempt 4
correctly recorded source commit `074f3c4d9ac8a02a5cf10cb1b2892ae7d81f59a6`,
but it converted the deferred `release-metrics` package into a current graph
node named `v0.2.9 reduced active metric set`. The final authority filter
excludes all deferred OpenSpec packages and admits only the approved Council
v0.3 and Node.js with TypeScript packages. Attempt 5 passed graph integrity
checks but still admitted five dated devlogs and the withdrawn v0.2.9 poster.
The final filter excludes those six historical inputs. No rejected candidate
changed the live graph.

## Rejected exact-main measurement

The first exact-main local attempt used a stale local `origin/main` ref. Its
log remains at
`/home/charl/.foreman/release-verify-v0282.DgRxge/ci-local-attempt1-stale-origin.log`
with SHA-256 digest
`8321f29eedba2f6f2e8f2f447625b0097d798b7b3737568fa8bdf8d13bc9b690`.
It is invalid release evidence. A later clean source-only checkout used exact
commit `074f3c4d9ac8a02a5cf10cb1b2892ae7d81f59a6` and exposed the launcher test
fixture defect that this branch corrects.

## Executed GitHub control-plane cleanup

- Pull requests #4 and #6 were closed because v0.2.9 is withdrawn and PR #12
  is the current release record.
- Pull request #5 was closed because Council pull requests #8, #9, #11, and
  #13 supersede it.
- Pull request #8 is merged through integration pull request #18.
- Pull requests #9, #11, #13, #15, and #17 were closed because merged pull
  request #18 contains their exact stack commits and all later integration
  work.
- Each closing comment names its replacement. No branch, commit, or tag was
  deleted.

## Not approved

No other tracked file, worktree, branch, tag, installed skill, database, formal
artifact, graph package, test, or generated graph is approved for deletion by
v0.2.8.2.

# George's Odyssey

![George's Odyssey release artwork](../../assets/v031-georges-odyssey.png)

Foreman v0.3.1 release candidate

Status: release candidate. The product measurements in this record bind to
pushed commit `64604d24308b446eaf1102177165622d3ec29167`. Pull request
[#45](https://github.com/CharlesHoskinson/foreman/pull/45) carries the final
candidate through the live release gate. This release is not yet merged,
tagged, or published.

v0.3.1 is one sentence: **session state is portable — one contract, two
implementations.**

## Product

`SessionStore` now has two complete implementations. SQLite remains the
reference implementation. `FilesOnlySessionStore` stores immutable NDJSON
generations and publishes a paired snapshot and projection outbox with one
`CURRENT` switch. Both implementations pass the same 49-case contract.

The CLI selects a backend through the port-owned factory. Production
TypeScript outside `packages/session-store/src` does not import `node:sqlite`
or concrete backend constructors. A boundary test enforces the rule and uses
negative controls for static, multiline, dynamic, and re-export forms.

`fm-session sync` drains a durable desired-state outbox with Effect-based
cancellation, timeout, and bounded retry. Entries use opaque version receipts,
so an acknowledgement cannot delete a newer operation for the same entity.
SQLite updates the row and outbox in one transaction. Files-only updates both
parts in one paired generation.

The delivery guarantee is durable idempotent at-least-once. It is not
exactly-once. A crash after remote success and before local acknowledgement
must replay the batch. A real `MemoryIndex` adapter must make repeated
desired-state operations idempotent. `NullMemoryIndex` remains the only
shipping adapter.

Snapshot import now supports an additive `remap` policy for a non-empty target.
It allocates new IDs per counted kind, rewrites fact and measurement pointers,
renames colliding sessions deterministically, preserves target rows and pending
receipts, and validates the complete result before one atomic commit.

## Exit predicates

All six predicates were measured on the unchanged pushed commit above.

| # | Predicate | Verdict | Evidence |
|---|---|---|---|
| 1 | No raw or concrete backend access outside the port | **PASS** | `npx tsx --test scripts/backend-boundary.test.ts`: 6 passed, 0 failed |
| 2 | CLI behaviour is unchanged | **PASS** | `bats tests/session.bats`: 29 passed; `fm-session-golden.test.ts`: 15 passed |
| 3 | The contract is portable | **PASS** | The unchanged 49-case suite passes for both backends; focused files pass 15/15 and 24/24 |
| 4 | Correctness is independent of projection | **PASS** | The I2 group passes 3/3; projection failure, timeout, and acknowledgement tests retain durable work |
| 5 | The migration is complete | **PASS** | `fm-session-main.ts` has 0 `ts-nocheck` occurrences |
| 6 | The outbox is durable, idempotent at-least-once | **PASS** | `outbox.test.ts`: 14 passed, including partial apply, timeout, ack failure, receipt race, and work bounds |

The conformance result has a working negative control. The do-nothing store
passes 25 of 49 cases and fails 24. Its failures span eight independent
categories: identity, import, obligation, outbox, retire, session,
supersession, and write. This result shows that the suite can reject a broken
backend.

## Verification

The complete verification round used the same commit.

| Check | Result |
|---|---|
| `npm run typecheck` | exit 0 |
| `npm test` | 1,665 tests: 1,661 passed, 4 skipped, 0 failed |
| `npx tsx --test packages/session-store/src/*.test.ts` | 171 passed, 0 failed |
| `npm run build` followed by `npm run verify-runtime` | deterministic rebuild; runtime verified |
| `npm run verify` | typecheck, the full test suite, runtime, register document, and DST-0060 smoke all passed |
| `npm run verify-install -- --skill-root skills/foreman` | 19 runtime artifacts passed manifest verification |
| `bash skills/foreman/scripts/docs-check.sh` | markdownlint, codespell, lychee, agent invocations, and comments passed |

The built `fm-session.js` is 700,615 bytes with SHA-256
`46f243f0ca21b78a72291f4a9d968be351d3ec989387e52dc87ed1810c3025dc`.
A second build produced the same digest and left the worktree clean.

## QA evidence block

This block is the release checklist. A local result is valid only for the tree
that produced it. GitHub Actions binds the required remote results to the exact
pull-request and `main` commit SHAs.

| Surface | Required evidence | Candidate result |
|---|---|---|
| Complete repository | `npm run verify` | 1,665 tests: 1,661 passed, 4 skipped, 0 failed; typecheck, runtime, register document, and DST-0060 smoke passed |
| Session store | `npx tsx --test packages/session-store/src/*.test.ts` | 171 passed, 0 failed |
| CLI compatibility | `bats tests/session.bats` and the golden test | 29/29 and 15/15 passed |
| Architecture boundary | `npx tsx --test scripts/backend-boundary.test.ts` | 6/6 passed, including negative controls |
| Projection drain | `npx tsx --test packages/session-store/src/outbox.test.ts` | 14/14 passed |
| Generated product | `npm run build`, `npm run verify-runtime`, and install verification | Deterministic bundle and 19 manifest artifacts verified |
| Documentation | `bash skills/foreman/scripts/docs-check.sh` | markdownlint, codespell, link checks, agent invocations, and comment checks passed |
| Pull-request gate | `gates-linux` on the exact head of pull request #45 | Required before merge |
| Integrated gate | `gates-linux` on the exact final `main` commit | Required before tag and publication |

No product check is skipped. Windows Bats is not a v0.3.1 gate and remains
BW-004. No external `MemoryIndex` adapter ships, so no live external projection
service is part of this release test matrix.

## What does not ship

No external `MemoryIndex` adapter or projection epoch protocol ships in this
release. That work remains assigned to v0.4.0 and must be tested against a live
service.

Files-only writer exclusion is a single-host process claim. It is not a network
filesystem lease. Deployments must use one writer and one drainer for a given
files-only directory.

The Windows Bats program remains BW-004 and is not a release gate. This release
does not claim that the full Bats suite passes on Windows.

## Review record

Foreman vendor lanes implemented the bounded tasks. Separate cold audits
blocked the projection outbox, remap, and SQLite boundary candidates until
their receipt, atomicity, writer-lock, migration, and failure-injection defects
were corrected. Tasks 4, 5, and 6 each received an independent `APPROVED`
verdict before its commit.

# Instruction improvement candidate

Candidate `bb0c1e9f3868d6bf36a91f78ceec55800192fc5c` meets the frozen instruction-reduction target.
It remains unaccepted as the completed release because the production-line target fails.
The [acceptance record](m6-instruction-acceptance.json) preserves exact source, measurement, package, and graph identities.

| Measure | Baseline | Current candidate | Result |
| --- | ---: | ---: | --- |
| Required instruction tokens | 16,502 | 8,122 | 50.78% reduction. Target met. |
| Production nonblank lines | 6,916 | 48,912 | 607.23% growth. Target not met. |

The default file plan now shows effects, exact models, controls, capabilities, scopes, gates, dependencies, and dynamic bounds in a concise human view.
Full `--json` output preserves the existing machine contract.
Mandatory worktree and role notes remove obsolete shell choreography and repeated instructions.
No counting rule, cohort, tokenizer, startup recipe, or threshold changed.

The complete startup trace uses the actual Grok serializer with a finite process fixture.
It records the transmitted prompt and schema and remains explicitly separate from live-account qualification.
The [first candidate](m6-candidate-acceptance.md) and its failed instruction measurement remain available.

Full verification passes with 3,205 passed tests, seven skipped tests, and no failures.
Runtime reproducibility, the build, and committed architecture checks pass.
Two package productions produce identical archive bytes.
All 19 installed-product checks pass, and the actual-archive configured-run fixture passes.

The advisory graph inventories all 2,660 tracked entries in this source candidate.
It contains 27,398 nodes and 57,120 edges.
Four syntax-warning files and 218 code paths without observed AST entities remain explicit.
Zero dangling endpoints does not establish complete extraction or normalization idempotence.
The separate qualified Graphify 0.9.48 graph remains unchanged.

The [source complexity audit](m6-source-complexity-audit.md) explains the larger implementation gap.
A scope question is pending about major redesign versus review of the measurement and scope definition.
The original production target remains in force.
Exact live coding readiness, numerical release reconciliation, and publication authority also remain unresolved.

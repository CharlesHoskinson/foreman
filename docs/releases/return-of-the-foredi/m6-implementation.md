# M6 adoption implementation

Status: implementation review and source verification are complete. Final candidate acceptance is in progress.
M6 acceptance remains open. Numerical version assignment and publication remain open.

## Installation and examples

The compiled package producer captures one clean Git candidate through a private index.
It rejects tracked changes, hidden index changes, and untracked source inputs.
Research and migration assets come from tracked candidate sources.
Ignored generated caches cannot replace those sources.

The package manifest binds payload paths, hashes, lengths, and modes.
The installer verifies the complete payload before selecting a retained version.
It applies declared file modes independently of the caller's umask.
The installed launcher is a symlink to compiled Node.js output.

The package contains eight workflow examples and six exact-profile examples.
Tests execute those programs through explicit finite provider fixtures.
Conditional execution starts a task before verification and review.
Task-only examples report missing delivery milestones instead of claiming delivery success.
Live account qualification remains separate from example execution.

See the [installation](../../guides/pel/install.md), [quickstart](../../guides/pel/quickstart.md), and [examples](../../guides/pel/examples.md) guides.

## Migration and history

The importer accepts exactly two recorded RoundPlanV1 and ExecutionContractV1 pairs.
It preserves original limits, host authority, identities, and required effects.
Unsupported constructs return precise source diagnostics.
The bounded rework fixture retains its six-action limit and refuses the seventh publication effect.

The twelve specified shell controllers and adapters are deleted.
The retained supervisor refuses automatic legacy restart before restoration, reservation, or queue submission.
Historical status reads original records without acquiring ownership or changing history.
Legacy records do not identify a verified original controller executable.
The diagnostic reports that missing information directly.

Council-specific execution remains outside this import cohort.
Historical decoding, credential isolation, ownership, containment, and publication authority remain required.
The [obligation record](adoption-obligations.json) retains the original release-program dependencies.

## Research and support

The portable research bundle contains immutable sources, source hashes, capture dates, claim classes, and coverage warnings.
Research queries can add bounded optional vault observations.
These observations have no capture timestamp and remain explicitly uncaptured.
Immutable `fm/research` execution refuses live vault observations.
Its read-result preparation uses no external action reservation.

Refresh validates the replacement before marking publication incomplete.
Atomic replacement preserves readable prior data after interrupted publication.
Advisory and qualified graph identities remain separate.

The Foreman vault has a dated implementation update with twelve immutable evidence captures.
The [transaction receipt](evidence/m6/vault-progress.json) lists all 22 changed paths.
Operation `foreman-foredi-implementation-progress-20260913` completed successfully.
The [read-only lint receipt](evidence/m6/vault-lint.json) records zero issues across 28 pages and 262 links.
The [f734 source-candidate graph](m6-graph-coverage.md) is complete. Later source changes require a new extraction.

Support export projects closed safe fields from existing records.
Its evidence kind comes from a validated immutable execution binding.
Unknown or legacy bindings remain `unknown`.
Package identity cannot promote a fixture run to product evidence.

Rollback checks the original project registry and active immutable program bindings.
Registration, admission, and failed-run revision share a short transaction lock under the original Foreman home.
Rollback holds that lock and then the installation lock through its compatibility check and version switch.
The protocol covers participating checkout and installed runtimes across prefixes.
Execution starts after both transaction locks are released.
Suspended and cancel-requested results remain active for compatibility checks.

The packaged quickstart fixture now uses actual project configuration services.
It stores configuration under the Git common directory and registers the original state root.
A bare run uses those settings.
Missing or invalid settings produce invalid-input outcomes without execution reservations.

## Qualification and execution corrections

Native coding qualification uses a disposable Git workspace and the actual containment boundary.
The host durably records each accepted permission decision before acknowledging it.
Final assessment occurs after transport cleanup and rejects ignored extra files or changed index history.
Qualification does not create product execution authority.

The actual Grok startup serializer exposed a misread provider constraint.
xAI accepts array bounds above 256 without guaranteeing server enforcement.
The adapter retains the declared bound and explicitly requires host validation.
The original candidate schema remains unchanged.

Race delivery selects the candidate from the durable winner lineage.
A joined loser receives cancellation only when its external outcome is known.
Unknown outcomes retain the existing recovery obligation.
Compiled crash fixtures cover interruption after winner selection and before the loser receipt.

The [fresh live qualification record](m6-live-qualification.md) records the exact attempted cells and their limits.
No native coding cell has current successful live evidence from this attempt.

## Acceptance still required

Final validation must use the rebuilt candidate and its produced archive.
The fixed measurement must report actual committed source counts and all failed targets.
The complete mandatory startup instruction trace is not yet bound.
Its required-token total and reduction ratio remain unknown.
The 40 percent glue and 50 percent instruction targets are not accepted.

Final graph refresh, release-program reconciliation, version assignment, and publication remain open.
Focused test results do not establish these outcomes.
## Final source verification

The [M6 verification record](m6-verification.json) retains the final source-test outputs and their hashes.
Full verification passes with 3,202 passed tests, seven skipped tests, and no failures.
The adoption suite passes all 139 tests.
Runtime build, reproducibility, installation integrity, and all six strict OpenSpec validations pass.
The [first candidate acceptance](m6-candidate-acceptance.md) records passing archive checks and failed simplification targets. Subsequent instruction changes require new candidate measurements. Numerical release reconciliation remains open.

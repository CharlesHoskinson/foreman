# ForeDi README: GPT6 Inkwell revision

Scope: `/home/charl/fm-wt/foreman-dsl-release/README.md`, only the Return of the ForeDi section. No other repository file was edited by this task. Artwork and the surrounding README remain unchanged. No commits, pushes, provider calls, or full test runs occurred.

## Editorial method

This is original English prose adapted using Inkwell’s live Grothendieck profile. The profile is diagnostic. Scientific accuracy and comprehension take priority over voice, and its numerical rates are not targets.

Read the existing Opus draft and `/tmp/foredi-opus-readme-report.md`. Read the Inkwell Grothendieck and writing skills, profile, metrics sidecar, English adaptation guide, and role instructions. Read repository AGENTS.md and the STE skill. Grounded Gottlieb patterns with the installed graphify 0.9.61 graph query `gottlieb_existing_pattern_1_review`. The initial ambiguous `Gottlieb` lookup was resolved by its exact node ID. No new corpus acquisition occurred.

Before editing, recorded the protected claims in `/tmp/foredi-claim-inventory.md`. The task sources were the Pel tutorial, semantics guide, compatibility profile, shipped implementation-and-review example, and 2026-09-13 scope amendment.

An existing Opus draft supplied the initial text. A new Zinsser draft was unnecessary. Dispatched an actual inherited GPT6 Gottlieb editor with the profile, guide, inventory, and audience. Findings are in `/tmp/foredi-gottlieb-findings.md`. Applied every critical and important finding. No profile override or dropped finding occurred.

Gottlieb found duplicated introductions, a grammar catalogue before the example, mixed mechanisms and benefits, and a historical C2 result presented as current-candidate success. The edit consolidates the motivation, puts command inspection before the example, moves detailed grammar into existing references, and restores evidence scope. It limits recovery wording to retained receipts and explicitly states that no executable K definition or proof exists.

The resulting structure motivates the language through plan/controller separation, defines the language/host boundary, demonstrates the dependency through a shipped program, and states open evidence and semantics questions. Technical names remain stable. The task adds no new scientific claim.

## Protected content

- Pel attribution, paper version, strict TypeScript/Node.js implementation, profile identity, and compatibility limitations remain explicit.
- Syntax needed for the example remains in the README. Full grammar details remain in the linked tutorial and compatibility profile.
- Host admission remains separate from static validation. Exact model, capability, path, and limit controls remain.
- The candidate, host verification, and independent review remain bound. Durable receipts preserve run identity and recovery evidence.
- C2’s 50.78 percent instruction reduction is historical. The final candidate needs fresh evidence against the retained 50 percent target.
- The original 40 percent production-code reduction failed. Its obligation is deferred to a separate, unnumbered cleanup release.
- Version assignment and final acceptance remain open. No speed, cost, or throughput measurement is claimed.
- M7 remains planned. No executable K definition or proof exists. Finite matching examples do not prove general equivalence.

## Validation

- The bundled CLI returned `valid Pel` for `examples/pel/implement-verify-review.pel`.
- Offline `foreman plan` returned the task → verify → review dependencies and bounded-dynamic preview. It dispatched no work.
- The README example matches the shipped example exactly.
- All 29 relative file/image links in the README resolve on disk.
- README prefix and suffix outside the owned section match the pre-edit snapshot byte-for-byte.
- `markdownlint-cli2 README.md` completed with zero issues.

## Le Guin and blind reader

Dispatched an actual inherited GPT6 Le Guin reviser after applying the Gottlieb edit. It read the profile, guide, inventory, and STE constraints. Findings are in `/tmp/foredi-leguin-findings.md`. The reviser found no remaining cadence defect and returned a byte-identical section. Necessary qualifications and repeated technical nouns stayed intact. No profile override occurred.

The reviser ran the installed KPI tool twice. Before/after sentence-length variance was 54.4. It ran the actual displacement tool and obtained exit 0, with every habit unchanged. Its first displacement invocation found no `ch*.md` files. The corrected invocation used `ch01.md` comparison files and produced `/tmp/foredi-leguin-displacement.txt`.

Dispatched the actual target-reader role with only the finished draft and audience, without profile, source notes, or editorial context. The first read returned one medium flag: the historical percentage lacked its baseline and counted instruction scope. Dispatched the Gottlieb editor in fix-cycle mode with that flag verbatim. Applied its baseline link, raw counts, and description of required workflow guidance. Kept detailed tokenizer metadata in the linked baseline instead of adding it to the README. The final full-draft reread returned `Verdict: CLEAR`. There are no unresolved reader flags. One fix cycle ran.

Reader evidence: `/tmp/foredi-target-reader.txt`. Fix note: `/tmp/foredi-reader-fix.md`. Final section: `/tmp/foredi-section-reader-fixed.md`.

## Actual diagnostics

VOICE: diagnostic (distance 0.5621878569609662, n=58)

The underlying `diagnostic_verdict` is `off`. The full uncalibrated panel is preserved in `/tmp/foredi-final-conformance.json`. The original whole-section score was diagnostic/off, distance 0.5633307141038233, n=55, in `/tmp/foredi-leguin-conformance.json`. One rescore followed the reader repair. Both invocations succeeded. No prose revision pursued the measured rates. The small adaptation corpus cannot establish author conformance, and the rhythm panel lacks its required 200 sentences.

The final displacement recheck returned exit 1. It reported repeated openers down 29 percent and fronted participles up 88 percent. Full output: `/tmp/foredi-final-displacement.txt`. Inspection of the actual regex matches explains the increase:

- Original match: “During execution, durable history records completed operations and their receipts.”
- Added match: “Required instruction tokens fell from 16,502 in the baseline to 8,122 at C2, a 50.78 percent reduction.”

The detector treats any opening word ending in `ing` or `ed`, followed soon by a comma, as a fronted participle. `During` is a preposition here. `Required` modifies the subject noun, and the first comma is a thousands separator. The added match is therefore a false positive, not a new fronted construction. The raw failure remains recorded. The final check is not represented as a pass, and the scientifically necessary measurement explanation remains intact.

A seven-word sequence comparison against both admitted English reference samples found zero shared sequences. The section does not copy distinctive source phrasing.

## Final verification

After the reader repair, repeated the local link, example-byte, outside-section preservation, Markdown lint, and diff-whitespace checks. All passed. The README has 29 resolving local file/image links. The final section remains the only repository area edited by this task. Broader release tests belong to the parent task and are not claimed as this task’s execution.

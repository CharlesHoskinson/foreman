{
  "verdict": "needs_revision",
  "summary": "The revised specifications resolve most initial findings. The resolved items include revision identity mapping, ArgSpec value presence, alreadyEmitted suspension, PEL_HOST_FAILURE, child invocation IDs, canonical wire values, the unified 15-tag ProviderFailure union, ProviderControlsV1, tool, cursor and observe contracts, prepareHostEffect reservation, the fm/verify schema, native-only fm/task, immutable source artifacts, automatic suspension records, the shared exit table, project configuration, install layout, the fixed migration corpus and the fixed measurement cohort. The page images confirm the triangle PIPE, the KEY class [a-zA-Z0-9_\\-+*/\\\\?!<>=.] and the SYMBOL exclusions [^\\s()\\[\\]\";|>], so the |> normalization is correctly declared. Concrete cross-milestone execution defects remain. Recoverable needs-action outcomes are delivered to M1 as terminal receipts. The M4 print handler contradicts the M1 descriptor and the paper. Role bindings have two sources. No module owns the fm/* descriptors behind the shipped snapshot. fm/retry :on is passed as quoted syntax rather than data. The reservation kind for retried nested effects is undefined. Race and retry child state has no journal record. Provider JSON output has no mapping to PelDataSchemaV1. A Pel final value has no mapping to run state. Revision can replenish counters. The fixture CLI entry collides with the root test glob. This is a specification audit only; nothing was executed.",
  "resolved_initial_ids": [
    "LANGUAGE-001",
    "LANGUAGE-002",
    "LANGUAGE-003",
    "LANGUAGE-004",
    "LANGUAGE-005",
    "LANGUAGE-006",
    "LANGUAGE-007",
    "LANGUAGE-008",
    "LANGUAGE-009",
    "LANGUAGE-010",
    "PLAN-011",
    "PLAN-012",
    "PROVIDER-013",
    "PROVIDER-014",
    "BUILD-015",
    "PLAN-016",
    "DELIVERY-017",
    "DELIVERY-018",
    "BUILD-019",
    "ADOPTION-020",
    "LANGUAGE-021",
    "LANGUAGE-022",
    "EXECUTION-001",
    "EXECUTION-002",
    "EXECUTION-003",
    "EXECUTION-004",
    "EXECUTION-005",
    "EXECUTION-006",
    "EXECUTION-008",
    "EXECUTION-009",
    "EXECUTION-010",
    "EXECUTION-011",
    "EXECUTION-012",
    "EXECUTION-013",
    "EXECUTION-014",
    "EXECUTION-015",
    "EXECUTION-016",
    "EXECUTION-018",
    "EXECUTION-019",
    "EXECUTION-020",
    "EXECUTION-021",
    "EXECUTION-022",
    "RELEASE-001",
    "RELEASE-003",
    "RELEASE-004",
    "RELEASE-005",
    "RELEASE-006",
    "RELEASE-007",
    "RELEASE-008",
    "RELEASE-010",
    "RELEASE-011",
    "RELEASE-012",
    "RELEASE-013",
    "RELEASE-014",
    "RELEASE-015",
    "RELEASE-016",
    "RELEASE-017",
    "RELEASE-018",
    "RELEASE-019",
    "RELEASE-020",
    "RELEASE-021",
    "RELEASE-022",
    "RELEASE-023",
    "RELEASE-024",
    "RELEASE-025",
    "RELEASE-026"
  ],
  "unresolved_initial_ids": [
    "EXECUTION-007",
    "EXECUTION-017",
    "PROVIDER-023",
    "RELEASE-002",
    "RELEASE-009"
  ],
  "findings": [
    {
      "id": "EXECUTION-101",
      "severity": "major",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-010",
        "R-M4-013",
        "R-M5-011",
        "R-M5-013",
        "R-M1-015"
      ],
      "problem": "M4 lists unknown-external-outcome, reconciliation-required and unconfirmed timeout as HostEffectFailure codes that map to recoverable needs-action. M1, however, treats any valid failure receipt as terminal for that request. The request leaves pending, the step becomes failed, and no exported function resumes a failed continuation. resumePel also rejects receipts for requests that are not pending. A later accept-result or confirmed observation, as in T-M4-013 and T-M5-013, therefore cannot continue evaluation. fm/publish without a grant has the same problem. Its prepared needs-action value is recorded as the completed result (step 4). After authority is granted, resume replays that recorded needs-action result, and publication would need a new run that redoes implementation.",
      "required_change": "Specify that recoverable outcomes are not delivered to M1. These are unknown-external-outcome, reconciliation-required, timeout with an unconfirmed remote outcome, and publication awaiting a grant. The request stays pending in the latest pel.suspension.v1 with a pel.effect.observed.v1 record, and the run state is needs-action. `foreman resume` then either re-prepares the effect or applies a PelRecoveryDecisionV1: accept-result supplies a success receipt, confirm-no-dispatch permits a reserved dispatch, and abandon delivers a terminal failure receipt. State which prepared needs-action values, if any, remain terminal Pel data.",
      "validation": "T-M4-013: accept-result drives the evaluator to done with zero redispatch. New T-M5-011 variant: grant authority, resume, and expect exactly one publish reservation and zero task or verify redispatch. T-M5-013 with available observation continues to a published result."
    },
    {
      "id": "EXECUTION-102",
      "severity": "major",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-020",
        "R-M1-010",
        "R-M1-017"
      ],
      "problem": "M4 says print 'returns nil under the registered nil result schema' and 'Print uses schema:pel-nil-v1'. M1 fixes the print descriptor as returning its input with result schema schema:pel-data-v1, and M1 defines no schema:pel-nil-v1. The paper (p.21 description and p.22 result => [\"hello\" \"Behnam\"]) returns vals unchanged. M4 must register its handler against the immutable M1 descriptor, which includes the result schema ID in the registry digest, so the M4 handler contract cannot be implemented.",
      "required_change": "Change M4 so print returns its :vals input under schema:pel-data-v1, matching the M1 descriptor. Update T-M4-020 to 'the exact registered data/Boolean schemas'.",
      "validation": "Construct the registry from the M1 print descriptor with the M4 handler. `(print :vals [\"hello\" name] :sep \" \")` returns [\"hello\" \"Behnam\"], and output is journaled once."
    },
    {
      "id": "EXECUTION-103",
      "severity": "major",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-001",
        "R-M5-008",
        "R-M5-016",
        "R-M2-014"
      ],
      "problem": "ForemanProjectV1 carries top-level roleBindings, nlConditionProfile and PelLimitsV1. It also references an immutable authoringSnapshot whose AuthoringSnapshotV1 contains roleBindings, nlConditionProfile and limits. `foreman check` and `foreman plan` resolve roles from the snapshot, while run admission derives the binding from the configuration. Nothing states which source wins or that the two must be equal. T-M5-008 changes only roleBindings in project-settings.json yet expects a changed snapshot digest. The referenced snapshot artifact is immutable and unchanged, so that expectation cannot hold.",
      "required_change": "Make one source authoritative. Recommended: remove roleBindings, nlConditionProfile and PelLimitsV1 from ForemanProjectV1 so they come only from the referenced snapshot, allowing only narrowing limits in the configuration. Alternatively, derive the admitted snapshot from the configuration and hash it. Reject a mismatch with exit 2.",
      "validation": "Admit a configuration whose roleBindings differ from its snapshot: exit 2 or a documented derivation. T-M5-008 changes the role pair through the specified source and observes changed snapshot and binding digests with identical preview and execution pairs."
    },
    {
      "id": "EXECUTION-104",
      "severity": "major",
      "file": "openspec/changes/foredi-05-task-delivery/design.md",
      "requirement_ids": [
        "R-M2-013",
        "R-M2-014",
        "R-M5-001",
        "R-M4-001"
      ],
      "problem": "Registry content for the default authoring snapshot has no owning source. M2 creates and ships default-authoring-snapshot.json with a full HostRegistryV1, and its examples call fm/task, fm/verify and fm/review. Those descriptors and their result schemas are defined only in M5 (makeForemanHostRegistry 'extends M2 descriptors'), and fm/race, fm/retry and fm/checkpoint only in M4. No task generates the snapshot from these registrations, and no admission check verifies that snapshot descriptors match the executable handler registry. schema:candidate-v1, used as the fm/task :output schema, has no defined content.",
      "required_change": "Name one pure TypeScript module that exports every Foreman descriptor spec and data schema, including schema:candidate-v1. Handler registration imports these specs. Generate or verify default-authoring-snapshot.json from that module in scripts/build-runtime.ts. At admission, require every snapshot registry ID to have a handler whose descriptor digest matches, otherwise exit 2.",
      "validation": "A build test regenerates the snapshot and it byte-matches the packaged asset. Admitting a snapshot with an altered fm/task ArgSpec or result schema exits 2 with zero dispatch."
    },
    {
      "id": "EXECUTION-105",
      "severity": "major",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-009",
        "R-M1-012"
      ],
      "problem": "M4 says the fm/retry :on value is 'a quoted keyword list, such as '[:rate-limited :transport-disconnected]'. Under M1, quote returns a syntax value with pair formation disabled. Syntax is excluded from PelDataValue and from host-safe arguments, so the documented argument is a syntax value, not a list of keys. The unquoted form [:rate-limited :transport-disconnected] would form nil-valued pairs instead. T-M4-009 uses the quoted form.",
      "required_change": "Specify :on as an evaluated list of quoted keys, `[':rate-limited ':transport-disconnected]`, which yields a list of key values. Alternatively, declare :on with evaluation \"syntax\" and define its decoding. Update the example and T-M4-009 to match.",
      "validation": "An M2 check of the T-M4-009 program passes. A decoder test accepts [':rate-limited] as a key list, and '[:rate-limited] fails with PEL_TYPE or the declared rule."
    },
    {
      "id": "EXECUTION-106",
      "severity": "major",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-009",
        "R-M4-019"
      ],
      "problem": "The retry wrapper reserves no action, and every nested external effect goes through prepareHostEffect. That step maps the fm/task call-site :id through taskActions to implement or correct. The nested fm/task in attempt 2 has a new request ID (parent/retry/2) and a new effect ID, so it reserves implement again. Nothing tells preparation that the effect is a retry of 'the same logical operation', and no such key is defined. M4 also keeps a separate effectId 'retry ordinal' that links a prior effect ID, but no mechanism produces a nonzero ordinal. T-M4-009 and T-M4-019 require one provider_retry charge with no implement recharge.",
      "required_change": "Pass the one-based attempt index in HostContextV1. Define the logical-operation key as parent retry request ID plus the nested invocation path suffix without the /retry/N segment. Nested external effects in attempt 2 or later with a matching key reserve provider_retry. Either remove the effectId retry ordinal or define it as this attempt index with its prior-effect link.",
      "validation": "Ledger spy on T-M4-009: attempt 1 reserves implement and attempt 2 reserves provider_retry only. A retry body with two nested effects charges per key."
    },
    {
      "id": "EXECUTION-107",
      "severity": "major",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-008",
        "R-M4-009",
        "R-M1-016",
        "R-M4-004"
      ],
      "problem": "Several durable-state gaps affect retry and race children:\n- The payload list has no record for child invocation state: started attempt index, allocated contender worktree grant, per-child counter debit tranche, abandonment, and the race winner 'committed journal decision'.\n- pel.suspension.v1 stores a single continuation, and the recovery rule for in-flight race or retry children after a crash is unstated.\n- fm/race and fm/retry bound arguments contain closures, but canonical PelDataValue JSON excludes closures while intents record canonical argument or preparation digests.\n- M4 says wrappers hold no worktree lock but does not say they release concurrency permits, so a race under maxConcurrentEffects=1 can deadlock.",
      "required_change": "Add typed payloads, for example pel.child.v1 (parent request ID, child invocation ID, grant, debit, disposition) and pel.race.decision.v1. Define recovery as re-running evaluateClosure from the parent suspension with journaled nested receipts, reusing recorded grants and deduplicating debits by child invocation ID. Define a canonical encoding for closure-valued arguments (code reference plus environment digest). State that wrappers hold no concurrency permit while children run.",
      "validation": "Crash after contender 1 completes and before the winner decision. Resume allocates no new worktree, performs zero redispatch of completed nested effects, debits once, and records one decision. A race with maxConcurrentEffects=1 terminates."
    },
    {
      "id": "EXECUTION-108",
      "severity": "major",
      "file": "openspec/changes/foredi-03-model-adapters/design.md",
      "requirement_ids": [
        "R-M3-009",
        "R-M3-019",
        "R-M5-001",
        "R-M4-020"
      ],
      "problem": "ProviderRequestV1.outputSchema has no defined type. Host result schemas are M1 PelDataSchemaV1 over Pel values, and the association type rejects wrong field order. Providers return JSON objects whose key order is not guaranteed. The specs define no deterministic lowering from PelDataSchemaV1 to each provider's JSON Schema subset. They also define no decoding of JSON objects, arrays, null and numbers into association, list, nil and key values. validateFinal, the {pelSource} envelope, the Boolean predicate and task outputs therefore cannot be validated or turned into receipts, and valid outputs may be rejected on key order.",
      "required_change": "Define outputSchema as a PelDataSchemaV1 ID plus content. Specify its lowering to JSON Schema under each profile's documented subset. Specify decoding JSON to PelDataValue: an object becomes an association in schema field order, key names become keywords exactly, duplicate and unknown keys are rejected, null becomes nil, and numbers follow the safe or finite rules. Apply the decoding before host validation.",
      "validation": "T-M3-009 fixture: JSON with reversed key order decodes to canonical order and passes. An unknown key fails OutputInvalid with a field path. The {pelSource} and Boolean envelopes round-trip."
    },
    {
      "id": "EXECUTION-109",
      "severity": "major",
      "file": "openspec/changes/foredi-05-task-delivery/design.md",
      "requirement_ids": [
        "R-M5-009",
        "R-M4-015"
      ],
      "problem": "The repair example returns a Pel-constructed list that M5 labels schema:delivery-needs-action-v1, and M5 says 'M4 projects this final value to needs-action exit 3'. M4 defines no validation of the final done value against any schema, and RunResultV1 has no final-value classification rule. The run state therefore depends only on unmet milestones. A contract without an audit or publication milestone would exit 0 for correction-limit or no-product-change, contradicting T-M5-009. This applies, for example, to the migrated bounded-rework case with publication disabled.",
      "required_change": "Add a binding field such as ExecutionBindingV1.finalResultSchemaIds. Define the rule that a done value validating as delivery-needs-action-v1 maps to needs-action exit 3, and invalid final values keep the milestone-derived state. Alternatively, state that needs-action comes only from unmet milestones and require the audit milestone for repair programs.",
      "validation": "T-M5-009 with a contract lacking audit and publication milestones still exits 3 for both counterfixtures. A program without that schema binding returning a needs-action-shaped list follows the documented rule."
    },
    {
      "id": "EXECUTION-110",
      "severity": "major",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-014",
        "R-M1-020",
        "R-M1-011"
      ],
      "problem": "A revised source starts fresh through startPel(program, environment, limits), and M1 provides no input to seed committed counters. M4 says re-evaluation 'restores those consumed counters' and uses a separate replay-fuel allowance, but no M1 API or M4 record implements either. Reductions and iterations consumed after the last pel.suspension.v1, such as the failing third expression, appear in no durable record. Revision therefore replenishes that consumption, although M4 states that revision cannot replenish execution counters.",
      "required_change": "Add committed-counter carry-over to M1, for example an initialCounters or committedDebit field in the start context. Persist failed-step counters in a pel.* record. Define how replay fuel is counted relative to maxReductions and maxIterations.",
      "validation": "T-M4-014 and T-M1-020: after a valid revision, committed reductions and iterations equal the old total (including the failed expression) plus the new suffix work. Revised evaluation cannot exceed the original limits."
    },
    {
      "id": "EXECUTION-111",
      "severity": "major",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-021",
        "R-M4-001",
        "R-M5-008",
        "R-M6-002"
      ],
      "problem": "The fixture CLI entry has several build problems:\n- The entry packages/orchestration/src/pel-cli-fixture-main.test.ts matches the root npm test glob \"packages/orchestration/src/**/*.test.ts\", so node --test would execute CLI main.\n- The same path is also the T-M4-021 test target.\n- No npm script builds dist-test/pel-cli-fixture.js before T-M4-001, T-M5-008 and T-M6-002 invoke it, so npm test and npm run verify fail on a clean checkout.\n- The fixture bundle resolves the default snapshot 'relative to its installation root', and no asset copy into dist-test is defined.",
      "required_change": "Move the entry to a path outside the test glob, for example packages/orchestration/src/test-support/pel-cli-fixture-main.ts, excluded from production entries. Put T-M4-021 in a separate *.test.ts file. Add a TypeScript build script, invoked before fixture-dependent tests and by verify, that bundles the entry and copies the assets.",
      "validation": "On a clean checkout, `npm test` runs the fixture acceptance tests without a manual esbuild step, and the root glob never runs CLI main. The production manifest excludes the entry."
    },
    {
      "id": "EXECUTION-112",
      "severity": "minor",
      "file": "openspec/changes/foredi-02-plan-authoring/specs/foredi-02-plan-authoring/spec.md",
      "requirement_ids": [
        "R-M2-008"
      ],
      "problem": "T-M2-008 expects successful JSON generation 'returning 3'. The M2 design states that authoring commands do not use 3, and success is 0.",
      "required_change": "Change T-M2-008 in the spec and catalog to exit 0 for a successful generation.",
      "validation": "Catalog lint compares authoring test exit codes with the M2 exit table."
    },
    {
      "id": "EXECUTION-113",
      "severity": "minor",
      "file": "openspec/changes/foredi-06-adoption/specs/foredi-06-adoption/spec.md",
      "requirement_ids": [
        "R-M6-002",
        "R-M6-019",
        "R-M4-021"
      ],
      "problem": "T-M6-002, T-M6-019 and the M6 design invoke `pel-cli-fixture.js project configure|run ...` without --fixture-manifest. M4 defines the exact fixture invocation with --fixture-manifest FILE, and the recorded Layer and manifest-bound state root depend on it.",
      "required_change": "Either add --fixture-manifest to the M6 commands, or specify fixture-launcher behavior when the manifest is absent (for example, exit 2).",
      "validation": "Every catalog fixture command matches the M4 invocation grammar."
    },
    {
      "id": "EXECUTION-114",
      "severity": "minor",
      "file": "openspec/changes/foredi-03-model-adapters/specs/foredi-03-model-adapters/spec.md",
      "requirement_ids": [
        "R-M3-002",
        "R-M3-021"
      ],
      "problem": "T-M3-002 expects UnsupportedCapability for 'Gemini minimal'. The closed ProviderControlsV1 effort enum has no 'minimal', so the value fails as an unknown enum during decoding before profile validation. Rejections of in-enum values implied by the profile table have no negative fixtures: Grok none, Gemini none/xhigh/max, Opus/Fable none.",
      "required_change": "State that 'minimal' is rejected at decoding with its field path. Add the negative fixtures for the in-enum disallowed efforts of each profile.",
      "validation": "The T-M3-002 table covers every enum value outside each profile's allowed set with exact field paths."
    },
    {
      "id": "EXECUTION-115",
      "severity": "minor",
      "file": "openspec/changes/foredi-03-model-adapters/tasks.md",
      "requirement_ids": [
        "R-M3-025",
        "R-M2-010",
        "R-M6-019"
      ],
      "problem": "Ownership and naming still conflict in three places. M3 tasks 'Create src/{errors,index}.ts', while M2 owns creation of the canonical union in packages/providers/src/errors.ts. The release README lists M5 as depending on M4 only, while the M5 catalog lists M1\u2013M4. M6 names the configuration ProjectConfigurationV1, while M4 names it ForemanProjectV1.",
      "required_change": "Change the M3 task to extend errors.ts. Align the README dependency column with the catalogs. Use one configuration type name.",
      "validation": "Only one tasks.md creates each providers file. A catalog and README consistency check passes."
    },
    {
      "id": "EXECUTION-116",
      "severity": "minor",
      "file": "openspec/changes/foredi-04-durable-execution/design.md",
      "requirement_ids": [
        "R-M4-001",
        "R-M6-017"
      ],
      "problem": "M6 requires that an unregistered explicit state-root override be registered before a run starts, so that rollback sees it. The M4 run --state-root override performs no such check. M4's configure step writes M6's <prefix>/state-roots.json, but M4-stage and fixture runs define no installation prefix.",
      "required_change": "Specify the registration check on the run and resume overrides, and the prefix resolution (or skip rule) for uninstalled and fixture execution.",
      "validation": "Run with an unregistered --state-root exits 2 or registers per the rule. A T-M6-017 root created through an override is checked."
    },
    {
      "id": "EXECUTION-117",
      "severity": "minor",
      "file": "openspec/changes/foredi-04-durable-execution/specs/foredi-04-durable-execution/spec.md",
      "requirement_ids": [
        "R-M4-003",
        "R-M4-019"
      ],
      "problem": "T-M4-003 requires the journal order suspension, preparation, reservation reference, intent, observation, result. The payload type list has no preparation record. Design step 6 appends the preparation digest and reservation reference with the intent. Reuse and needs-action preparations have no record type.",
      "required_change": "Either add a pel.effect.preparation.v1 payload, or change T-M4-003 to the actual record sequence.",
      "validation": "A decoder test over the T-M4-003 journal matches the declared payload types."
    },
    {
      "id": "EXECUTION-118",
      "severity": "minor",
      "file": "openspec/changes/foredi-01-pel-language/design.md",
      "requirement_ids": [
        "R-M1-019"
      ],
      "problem": "Several classification-matrix locators are wrong:\n- Nil (\u00a74.2) is cited as \u00a74.1.\n- `if` (\u00a74.6.1) is cited as \u00a74.1.\n- Errors (mixed arguments, \u00a74.3 p14) are cited as \u00a74.1.\n- Keyword precedence (KEY regex, \u00a74.1 p11) is cited as \u00a74.6.\n- Sequence arity (\u00a73 p9 and \u00a74.6.4 p18) is cited as \u00a74.1.\n- Pair formation (\u00a74.2 p12) is cited as \u00a74.6.",
      "required_change": "Correct the paperSection and paperPage values in the matrix and in paper-v2.json.",
      "validation": "A reviewer spot-checks the locators against the rendered pages."
    }
  ],
  "strengths": [
    "The revision flow re-evaluates fresh source with mapped receipts under new request IDs and rejects partial-form edits before dispatch.",
    "prepareHostEffect with a single-use reservation token cleanly separates reuse, needs-action and dispatch charging across M4 and M5.",
    "M3 observe/RemoteObservationV1, the tool and cursor journal records and ToolResultV1 content keep adapters free of storage and redispatch.",
    "Cancellation now has a finite observation window with an exact mapping from remote outcome to external outcome and exit code.",
    "A single 15-tag ProviderFailure union with fixed retry classes is shared by the M2 and M4 exhaustive switches.",
    "admitCell requires live evidence for the exact product capability, and test-fixture evidence cannot become live-qualified.",
    "The repair example is valid bounded self-recursion under M1 semantics, with worked dispatch counts."
  ],
  "coverage": {
    "milestones_read": [
      "M1",
      "M2",
      "M3",
      "M4",
      "M5",
      "M6"
    ],
    "limitations": [
      "Specification audit only; no runtime, commands, builds or openspec validation were executed.",
      "Existing sources (execution-ledger.ts, run-journal.ts, verify-runtime.ts, tsconfig.base.json) were not supplied, so ledger action kinds and manifest schema changes were not verified against code.",
      "Provider behavior is taken from dated evidence summaries; no live model behavior was tested.",
      "PDF pages 11, 14, 18, 20, 21 and 22 were checked visually; other pages relied on the text capture and PixelRAG record."
    ]
  }
}

# v0.2.9.0 cleanup and destruction log

Register each destructive action before execution. Preserve tracked content in Git before removal.

## Current register

| ID | Target or action | Disposition |
|---|---|---|
| `DST-0030` | Withdrawn v0.2.9 worktree at `04f3695` | `deferred` until untracked `SPEC.md` has an owner and disposition |
| `DST-0031` | Worker-created root package files from task 167 | `executed_reversible` because the files moved to `.harness/task167-root-package-junk/` |
| `DST-0032` | Primary worktree spec and harness artifacts | `executed_reversible` because the files moved to the quarantine path below |
| `DST-0033` | v0.2.8.2-only current roadmap and checklist | `executed_reversible` by the v0.2.9.0 authority commit |
| `DST-0034` | Live knowledge graph built before the candidate | `deferred` until an exact-candidate replacement passes validation |
| `DST-0035` | Four excluded OpenSpec packages with active v0.2.9 claims | `executed_recoverable` by this tracked cleanup; Git and `v0.2.9-preserve` retain them |
| `DST-0036` | External dogfood dependency tree from failed task 177 | `executed_reversible` by moving it outside the target worktree |
| `DST-0037` | Foreman `.harness/` runtime paths | `protected` by a repository ignore rule; existing run records remain recoverable |
| `DST-0038` | Current primary and external dogfood run artifacts | `executed_reversible` to the quarantine path below |

## DST-0030 withdrawn worktree

The target identifier is
`foreman-wt-v029-release-20260802-implement-small-four` under the operator's
worktree root.

The branch head is `04f369541febf648ef39dcb8fedd930e8b600d3f`. The annotated tag `v0.2.9-preserve` contains this commit.

The worktree contains untracked `SPEC.md`. Do not remove the worktree until the file has a recovery destination.

Git can recover the withdrawn release notes with this command:

```bash
git show v0.2.9-preserve:docs/releases/v0.2.9.0-notes.md
```

## DST-0031 worker package files

Task 167 created `package.json`, `pnpm-lock.yaml`, and `pnpm-workspace.yaml` at the repository worktree root.

These files were untracked and worker-owned. They moved to `.harness/task167-root-package-junk/` before acceptance.

The Council workspace package files remain under `components/council/`. The quarantine files are not release inputs.

## DST-0032 primary run artifacts

The primary worktree contained untracked `.harness/` and `SPEC-PREFLIGHT-CLI.md` paths.

The accepted implementation commits and GitHub branches preserve the product changes. The run artifacts moved intact to this path:

`FOREMAN_QUARANTINE_ROOT/v029-preflight-primary-run-artifacts-a2d2dbd/`

The spec SHA-256 digest is `c7d13b185b13e520148a28ad5c5e2f7bc8e2b1b532123d7b64b59bc5e133a058`.

The heartbeat SHA-256 digest is `b3b1627254fa2ec2d0c1108470f0931522b013afcd7d03a62d65babbc5479c02`.

The stream SHA-256 digest is `623f09fe0376d5c6c698756ffac7e79dc7de913d492542c16e83da4c29d2b105`.

## DST-0035 stale OpenSpec packages

The cleanup package targets these directories:

- `openspec/changes/release-metrics/`
- `openspec/changes/graph-dogfood/`
- `openspec/changes/cross-vendor-audit-routing/`
- `openspec/changes/readme-refresh/`

These packages use active v0.2.9 wording from the withdrawn plan. `.graphifyignore` excludes them from current graph authority.

The cleanup removed the tracked copies. Git and `v0.2.9-preserve` retain their history.

## DST-0036 failed external dependency scan

Task 177 stalled while the source-secret scan traversed `node_modules` on a
Windows-mounted external worktree. The task was stopped through Foreman's queue
control. The dependency tree moved intact to this recovery path:

`FOREMAN_TMP_ROOT/v029-external-council-outcomes-20260803/node_modules-attempt177`

Task 178 then ran the source scan before dependency installation. Grok produced
commit `31e26ea`, and the target gate passed.

Do not delete the recovery path until the release evidence commit is on GitHub.

## DST-0037 harness runtime paths

Foreman creates `.harness/` telemetry and vendor-home state inside worktrees.
These paths are runtime state, not repository source. The v0.2.9.0 candidate
adds `.harness/` to the repository ignore rules.

Do not copy credentials into an unignored target path. A later TypeScript
orchestrator must define an external runtime-state root and explicit credential
provisioning.

## DST-0038 current run artifacts

The primary `.harness/`, external `.harness/`, and external
`FOREMAN_REPORT` files to this recovery directory before final documentation
validation:

`FOREMAN_QUARANTINE_ROOT/v029-release-current-run-artifacts-20260803/`

The product and evidence commits do not depend on these runtime files.

## Graph replacement rule

Do not replace `graphify-out/` until the candidate graph records the exact source commit. Validate duplicate and dangling records first.

Move the prior graph to quarantine before promotion. Record both graph digests and the recovery path.
